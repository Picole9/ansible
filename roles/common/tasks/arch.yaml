- name: install packages
  become: true
  community.general.pacman:
    name:
      - bat # cat
      - exa # ls
      - ripgrep # grep
      - fd # find
      - btop # htop
      - universal-ctags
      - ca-certificates
      - curl
      - git
      - gnupg
      - tldr
      - nodejs
      - npm
      - python-pip
      - python-virtualenv
