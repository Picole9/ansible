- name: Pacman upgrade
  tags: update
  become: true
  community.general.pacman:
    update_cache: true
    upgrade: true

- name: Install packages
  tags: install
  become: true
  community.general.pacman:
    update_cache: true
    name:
      - bat # cat
      - base-devel
      - bind # dnsutilities like nslookup
      - btop # htop
      - curl
      - eza # ls
      - fd # find
      - fzf
      - git
      - gnupg
      - jq # json
      - lazygit
      - logrotate
      - man-db
      - man-pages
      - nano
      - openbsd-netcat
      - openssh
      - python
      - python-pip
      - ripgrep # grep
      - wget
      - zoxide
