- name: Install packages
  become: true
  community.general.pacman:
    update_cache: true
    name:
      - bat # cat
      - btop # htop
      - ca-certificates
      - curl
      - eza # ls
      - fd # find
      - git
      - gnupg
      - python-pip
      - python-virtualenv
      - ripgrep # grep
      - otf-hasklig-nerd
