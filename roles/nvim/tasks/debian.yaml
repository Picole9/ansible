- name: Install packages
  block:
    - name: Add repo
      ansible.builtin.apt_repository:
        repo: "ppa:neovim-ppa/unstable"
        state: present
      become: true
    - name: Install packages
      ansible.builtin.apt:
        name:
          - curl
          - fd-find
          - fzf
          - git
          - gzip
          - neovim
          - npm
          - python-pynvim
          - ripgrep
          - tar
          - unzip
        state: present
        update_cache: true
      become: true
